paths:
  uploads: uploads

files:
  index.html:
    editor:
      inputs:
        heading:
          type: text
          label: Page Title
          selector: h1
        description:
          type: textarea
          label: Page Description
          selector: p

collections:
  html_pages:
    label: HTML Pages
    path: /
    files:
      - index.html
